#! /usr/bin/perl -w

# Perlscript that work as the cat command in unix like systems

use strict;

while (<>) { # Diamond operator for input, put in default variable $_
    print; # If no argument print default variable
}
